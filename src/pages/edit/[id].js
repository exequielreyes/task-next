//para que se devuelva lo que ya tenemos escrito para editar hacemos:
// con el export default TaskFormPage devuelve la misma pagina de new pero en [id]
import newTaskForm from "../new";

export default newTaskForm;